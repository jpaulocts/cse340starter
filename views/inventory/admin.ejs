<!--Content heading-->
<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>
    <!--Message Block-->

    <%- messages() %>
    <% if (errors) { %> 
        <ul class="notice">
           <% errors.array().forEach(error => { %>
            <li><%= error.msg %></li><%}) %>       
        </ul>
    <%} %>
   
    <!--Inventory display-->
<section> 
   <h2>New classes added</h2>
    <%- div %>

</section>
<section>
    <h2>New vechicles added</h2>
    <%- grid %>

</section>